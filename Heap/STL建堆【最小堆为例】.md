# STL建堆【最小堆为例】

```c++
priority_queue<int> q_max;//大根堆
priority_queue<int,vector<int>,greater<int> >q_min;//小根堆

/*
    q.top()

    q.push()
    q.pop()

    q.empty()
    q.size()
*/
```

